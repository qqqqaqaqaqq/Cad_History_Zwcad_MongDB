<UserControl x:Class="CadEye_WebVersion.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CadEye_WebVersion.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid
        Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>
        <Grid
            Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <RadioButton
                x:Name="DarkMode"
                Grid.Column="0"
                Margin="20"
                Command="{Binding OnThemeToggle.ThemeToggleCommand}"
                Foreground="{Binding DataContext.Theme, RelativeSource={RelativeSource AncestorType=Window}}"
                CommandParameter="DarkMode"
                Background="Wheat" 
                Content="DarkMode"/>
            <RadioButton
                x:Name="WhiteMode"
                Grid.Column="1"
                Margin="20"
                Command="{Binding OnThemeToggle.ThemeToggleCommand}"
                Foreground="{Binding DataContext.Theme, RelativeSource={RelativeSource AncestorType=Window}}"
                CommandParameter="WhiteMode"
                Background="Wheat" 
                Content="WhiteMode"/>
        </Grid>
    </Grid>
</UserControl>
